require "spec_helper"

describe "Login" do
  it "should authenticate users" do
    sleep(5)
    expect(2).to eq(2)
  end

  it "should restrict access" do
    sleep(5)
    expect(2).to eq(2)
  end
end
